<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deloitte.bdh.data.analyse.dao.bi.BiUiDemoMapper">

    <!-- 查询表字段 -->
    <select id="selectDemoList" parameterType="String" resultType="java.util.LinkedHashMap">
        ${querySql}
    </select>

    <select id="selectCount" parameterType="String" resultType="Long">
        ${countSql}
    </select>
</mapper>
